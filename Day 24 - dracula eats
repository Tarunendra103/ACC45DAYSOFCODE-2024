def count_tuesdays(N):
    if N < 2:
        return 0
    return (N - 2) // 7 + 1

# Input reading
T = int(input())
results = []
for _ in range(T):
    N = int(input())
    results.append(count_tuesdays(N))

# Output results
for result in results:
    print(result)
