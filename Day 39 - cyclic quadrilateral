# Read number of test cases
T = int(input())

# Process each test case
for _ in range(T):
    # Read the four angles
    A, B, C, D = map(int, input().split())
    
    # Check if the quadrilateral is cyclic
    if A + C == 180 and B + D == 180:
        print("YES")
    else:
        print("NO")
