
T = int(input())


for _ in range(T):
    # Read the prices A, B, C
    A, B, C = map(int, input().split())
    
    # Calculate the total cost
    total_cost = A + B + C - min(A, B, C)
    
    
    print(total_cost)
